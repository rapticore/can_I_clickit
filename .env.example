# ──────────────────────────────────────────────────────────────
# Can I Click It? — Backend Environment Variables
# Copy to backend/.env (or export in your shell) and fill in values.
# All variables are prefixed with CLICKIT_ in the environment.
# ──────────────────────────────────────────────────────────────

# ── Core ─────────────────────────────────────────────────────
# CLICKIT_DEBUG=true              # Enable debug mode (optional, default: false)
# CLICKIT_LOG_LEVEL=INFO          # Log level (optional, default: INFO)

# ── Database (required) ─────────────────────────────────────
CLICKIT_DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/clickit

# ── Cache (required) ─────────────────────────────────────────
CLICKIT_REDIS_URL=redis://localhost:6379/0

# ── API Keys (required in production) ────────────────────────
# JSON list of valid API keys for authenticating requests.
# In debug mode, an empty list allows unauthenticated access.
CLICKIT_API_KEYS='["your-api-key-here"]'

# ── CORS (optional, default: []) ─────────────────────────────
# JSON list of allowed origins. Wildcard "*" is rejected when debug is false.
CLICKIT_CORS_ORIGINS='["http://localhost:3007"]'

# ── JWT Authentication ─────────────────────────────────────────
# Required in production. In debug mode, a dev placeholder is used.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# CLICKIT_JWT_SECRET_KEY=your-secret-key-here
# CLICKIT_JWT_ALGORITHM=HS256           # (default: HS256)
# CLICKIT_JWT_EXPIRY_MINUTES=1440       # (default: 1440 = 24h)

# ── AI / LLM ─────────────────────────────────────────────────
# CLICKIT_ANTHROPIC_API_KEY=      # Anthropic API key (optional)
# CLICKIT_ANTHROPIC_MODEL_FAST=claude-3-5-haiku-20241022
# CLICKIT_ANTHROPIC_MODEL_COMPLEX=claude-sonnet-4-20250514

# ── Threat Intelligence (optional) ───────────────────────────
# CLICKIT_VIRUSTOTAL_API_KEY=     # VirusTotal API key
# CLICKIT_PHISHTANK_API_KEY=      # PhishTank API key

# ── Feature Flags (optional) ─────────────────────────────────
# CLICKIT_ENABLE_LIVE_LINK_CHECKS=false   # Enable live URL lookups (default: false)
# CLICKIT_MAX_LIVE_URL_LOOKUPS=2          # Max URLs to check live per scan
# CLICKIT_FREE_TIER_DAILY_SCANS=5         # Daily scan limit for free tier
# CLICKIT_CACHE_TTL_SECONDS=2592000       # Cache TTL in seconds (default: 30 days)

# ── AWS / S3 (optional) ──────────────────────────────────────
# CLICKIT_AWS_REGION=us-east-1
# CLICKIT_S3_BUCKET=clickit-data

# ── Search (optional) ────────────────────────────────────────
# CLICKIT_ELASTICSEARCH_URL=http://localhost:9200
